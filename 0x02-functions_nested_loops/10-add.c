#include "main.h"

/**
* add - check the code
* @int: a function that adds two integer and returns the result
* Return: always 0
*/

int add(int, int)

{
	int a;
	int b;

	{
		n = add(a, b);
		printf("d\n", n);
	}

	return (0);
}
