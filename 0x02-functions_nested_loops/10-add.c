#include "main.h"

/**
* add - check the code
* @a: a function that adds two integer and returns the result
* Return: always 0
*/

int add(int a ; int b)

{
	int n;

	{
		n = add(a, b);
		printf("d\n", n);
	}

	return (0);
}
